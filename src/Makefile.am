bin_PROGRAMS = sim

sim_SOURCES = sim.cpp sim.h sim.glade

LIBS = $(LIBGLADE_LIBS) $(GTHREADS_LIBS) $(GTK_LIBS) $(MUPARSER_LIBS)
INCLUDES = $(LIBGLADE_CFLAGS) $(GTHREADS_CFLAGS) $(GTK_CFLAGS) $(MUPARSER_CFLAGS)

